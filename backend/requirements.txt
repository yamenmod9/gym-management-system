# Versions here are security floors as much as pins. The set this replaced
# carried known advisories in five packages that sit directly in the request
# path — see .github/dependabot.yml for why nothing was catching that.
Flask==3.1.3                  # PYSEC-2026-2151
Flask-RESTful==0.3.10
Flask-JWT-Extended==4.7.4     # needed for PyJWT >= 2.10
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.5
Flask-Cors==6.0.5             # PYSEC-2024-71, -260, PYSEC-2026-1383/1384/1385
Flask-Limiter==3.8.0
flask-marshmallow==1.2.1
marshmallow==3.26.2           # PYSEC-2026-1605. Deliberately not 4.x: that is
                              # a breaking rewrite, and the custom EnumValue
                              # field in app/schemas depends on 3.x behaviour.
marshmallow-sqlalchemy==1.5.0
passlib==1.7.4
python-dotenv==1.2.2          # PYSEC-2026-2270

# The IANA time zone database. `zoneinfo` otherwise reads the host's, which
# Alpine images and Windows do not have — and without it every per-gym business
# day lookup (daily closing, daily sales, accountant dashboard) raises.
tzdata==2026.3
SQLAlchemy==2.0.36
gunicorn==23.0.0              # CVE-2024-1135 request smuggling, fixed in 22.0

# Signs and verifies every access token in this system. Was unpinned at
# ">=2.0.0" and resolving to 2.9.0, which carries nine advisories.
PyJWT==2.13.0
sentry-sdk[flask]==2.17.0
firebase-admin>=6.0.0
geoip2==4.8.0

# PostgreSQL driver (Railway's managed Postgres addon).
# 2.9.9 ships no wheels past Python 3.12, so it fell back to building from
# source — which needs libpq headers and fails on a plain developer machine.
psycopg2-binary==2.9.12
